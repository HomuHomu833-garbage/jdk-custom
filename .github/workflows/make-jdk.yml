name: Build JDK (reusable)
on:
  workflow_call:
    inputs:
      platform:
        description: "linux | bsd | windows | macos | android"
        required: true
        type: string
      targets:
        description: "JSON array of target triples"
        required: true
        type: string
      jdk_version:
        description: "OpenJDK feature version"
        required: true
        type: string
      ndk_version:
        description: "Official NDK version for the android clang (android only)"
        required: false
        type: string
        default: ""
      ndk_revision:
        description: "Official NDK revision letter (android only)"
        required: false
        type: string
        default: ""

permissions:
  contents: write
  packages: read

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(inputs.targets) }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5

      - name: Log in to GHCR
        uses: docker/login-action@v4.2.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build
        run: |
          IMAGE="ghcr.io/$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')/jdk-builder:latest"
          docker run --rm \
            -v "$PWD:/work" -w /work --user "$(id -u):$(id -g)" \
            -e HOME=/work \
            -e PLATFORM='${{ inputs.platform }}' \
            -e TARGET='${{ matrix.target }}' \
            -e JDK_VERSION='${{ inputs.jdk_version }}' \
            -e NDK_VERSION='${{ inputs.ndk_version }}' \
            -e NDK_REVISION='${{ inputs.ndk_revision }}' \
            -e ROOTDIR=/work \
            "$IMAGE" \
            bash -c 'scripts/fetch-source.sh && scripts/build.sh && scripts/make-jdk.sh'

      - name: Release
        uses: ncipollo/release-action@v1.21.0
        with:
          tag: "${{ inputs.jdk_version }}"
          artifacts: jdk-${{ matrix.target }}.${{ inputs.platform == 'windows' && '7z' || 'tar.xz' }}
          allowUpdates: true
          replacesArtifacts: true
          body: ''
