# Force LF for all shell/docker scripts
scripts/* text eol=lf
*.sh text eol=lf
Dockerfile text eol=lf
# Patches are fed to `git apply` against an LF source tree; CRLF breaks them.
*.patch text eol=lf
# Sources copied into the JDK tree and compiled by the cross toolchains.
*.c text eol=lf
